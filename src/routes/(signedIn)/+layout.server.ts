// import type { LayoutServerLoad } from './$types'

// export const load: LayoutServerLoad = async (event) => (console.log('load server: event.locals.session', event.locals.session.data)||{
// 	session: await event.locals.session.data,
// })
